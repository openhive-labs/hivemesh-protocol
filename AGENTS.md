# Repository Guide

Read `.agents/rules/README.md` before changing protocol material.

- Human-readable specifications live in `.agents/docs`.
- Protocol governance lives in `.agents/rules`.
- Machine-readable definitions live in `schemas`.
- Compatibility examples live in `test-vectors` and `conformance`.
- Implementation-specific behavior belongs in `openhive-labs/hivemesh`.
